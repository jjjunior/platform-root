<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>br.com.jstack.platform</groupId>
        <artifactId>spring-stack</artifactId>
        <version>1.0.85</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- Security Spring Boot module -->
    <name>starter-security-spring-boot</name>
    <artifactId>starter-security-spring-boot</artifactId>
    <packaging>pom</packaging>

    <!-- Project-wide properties -->
    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    </properties>

</project>
